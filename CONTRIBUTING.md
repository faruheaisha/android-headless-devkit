# 贡献指南

感谢有兴趣改进本 skill。本 skill 是一份**可执行的流程规范**，不是知识库，
因此对"事实准确性"和"可验证性"的要求高于一般文档。

## 两条硬性要求

### 1. 一切数值必须实测，不接受估算

本仓库里出现的每个数字（体积、耗时、内存、版本号、报错原文）都应当能追溯到
一次真实执行。写"大约 10 分钟"之前，先跑一遍；写"包体约 70MB"之前，先量一次。

**如果确实没有实测条件**，明确标注为"未验证"，不要让它看起来像实测值。

### 2. 脚本改动必须实跑

改 `scripts/` 或 `self_test.py` 后，至少执行：

```powershell
python .\self_test.py          # apk_report.py 的 23 项断言应全通过
python .\validate_skill.py     # SKILL.md 的 18 项结构断言应全通过
```

`validate_skill.py` 会检查 frontmatter 完整性与**引用无断链** ——
改过 `SKILL.md` 的目录树或文件名后尤其要跑它（重命名文件很容易漏改引用）。

涉及设备流程的改动（`verify_loop.ps1`），需要真跑一次装机验收：
先启动无头模拟器，再执行 `verify_loop.ps1`，确认截图非空、崩溃检查与
进程存活结论正确。

> 这不是形式要求。本仓库的 `verify_loop.ps1` 首版有两处真 bug
> （函数定义顺序、adb 输出未校验导致假阳性），**都是实跑才暴露的** ——
> 静态阅读代码不会发现。

## 目录约定

| 位置 | 放什么 | 不放什么 |
|---|---|---|
| `SKILL.md` | 入口：工作流、能力边界、硬性经验、待查清单 | 长篇方法论细节、可执行代码 |
| `references/` | 按阶段拆分的方法论全文 | 可执行代码 |
| `scripts/` | 可独立运行的脚本，不依赖私有数据或网络 | 业务逻辑、密钥 |
| `self_test.py` | 对 `scripts/` 的自动断言 | 需要真机才能跑的用例 |
| `evals/` | 触发与行为断言 | — |

## 写作风格

- **先给判据，再给做法**。"看到 X 就说明 Y"比"你可以尝试 Z"更有用。
- **症状 → 根因 → 解法**三段式组织坑点，便于检索。
- **保留报错原文**。原文比转述更有检索价值（别人会直接搜报错）。
- **写清"为什么这样做"**。尤其对于偏离直觉的做法
  （如"浅色底上的玻璃质感主要靠阴影而非透明度"）。
- 中文为主，技术名词保留英文原文。

## 新增坑点

坑点请加进 `references/06-pitfall-index.md` 对应分组，格式：

```
| 编号 | 症状（含报错原文关键词） | 根因 | 解法 |
```

若该坑点较深（需要展开代码或表格），在阶段文档里写详版，
速查表里放一行并链接过去。

## 提交前自检

- [ ] `python self_test.py` 全通过（23 项）
- [ ] `python validate_skill.py` 全通过（18 项，含引用无断链）
- [ ] 新增/修改的数值都是**实测**的，未实测的已标注
- [ ] 涉及设备流程的改动已**真跑过一次**装机验收
- [ ] ★ **已跑隐私审计**（见下节，四类模式 0 命中）
- [ ] 若改了 `SKILL.md` 的 `description` → 已过一遍 `evals/evals.json`
      （description 触发语境不全会导致 undertrigger，边界不写清会 scope creep）
- [ ] 变更已记入 `CHANGELOG.md`

## ★ 隐私审计（每次发布前必跑）

本 skill 来自真实项目的实践。写文档时为了把问题讲清楚，
**必然会带进来源项目的可识别信息** —— 这是自然发生的，不是疏忽。
所以必须设一道固定关卡，不能靠"我记得清理过"。

**主方式：跑脚本**（判据只有一份，不会出现"规则改了脚本没改"）：

```bash
# 公开仓库的默认用法
python scripts/privacy_audit.py --root .

# 本地/CI：注入自己的项目标识（真实值不进公开仓库，见下）
PRIVACY_MARKS="myproject,myalias" python scripts/privacy_audit.py --root .

# 退出码：0 干净 / 1 有命中 —— 可直接挂进 CI 或 pre-commit
```

脚本查四类：

| # | 类别 | 判据（要求"像真值"，不是"出现过关键词"） |
|---|---|---|
| ① | 密钥 | `sk-` 后 ≥16 位字母数字 —— 说明性文本 `sk-...` 不会命中 |
| ② | 项目标识 | 由 `PRIVACY_MARKS` 环境变量注入 |
| ③ | 本机绝对路径 | 用户名必须是真值；`<用户>` 这类占位符不算 |
| ④ | 中文目录路径 | 中文必须出现在**路径段内**；并放行文档里必要的反面示例 |
| ⑤ | 文件类型 | 混进来的 `.kt` / `.apk` / `.csv` 等一律报 |

### ★ 为什么 ② 用环境变量注入，而不是写在脚本里

这里有个绕不开的矛盾：**脚本需要知道"要查什么"，但脚本本身在公开仓库里。**
把真实项目名写进脚本，等于用脚本自己完成了泄漏。
所以公开的那份保持空，真实值从环境变量传 —— 本地和 CI 用得上，仓库里看不到。

### 为什么做成脚本，而不是文档里几条 grep

两条都踩过：

1. **判据必须只有一份。** 曾经 CONTRIBUTING 里的判据已收紧、
   而抽查脚本还用旧判据，于是报出假阳性 —— 规则改了、用规则的地方没改。
2. **正则在中文范围上极易写错。** 在 Python raw string 里写 `\u4e00`，
   不会解释成 unicode 转义，而会变成字符类 {反斜杠, u, 4, e, 0, a, 5}，
   于是 `E:\AndroidDev4` 这类普通路径也命中。用 `chr(0x4E00)` 拼区间最不容易出错。

> 这类"规则实现比规则本身更容易出错"的情况，值得把规则固化成代码而不是文档。

### ★ 为什么 ③ 用白名单，不用黑名单

一开始写成"排除几个已知源"的黑名单，结果徽章图片和标准文档链接
（`img.shields.io`、`agentskills.io` 等）全被报出来。
**一条会误报的规则等于没有规则** —— 跑两次全是噪音，第三次就没人跑了。
白名单的维护成本高一点，但它给出的每一条命中都值得看。

### ★ 为什么必须排除规则文档自身与审计脚本

`CONTRIBUTING.md` 与 `scripts/privacy_audit.py` 里必然写着上面这些模式，
不排除就**稳定产生假阳性**。这不是"规则文本不算泄漏"这么简单 ——
它意味着**审计输出里永远有固定噪音，训练使用者忽略它**。

### ★ 为什么保留"中文项目路径"这个反面示例

文档里用 `D:\工作\我的项目pp` 举例说明"中文路径会导致 Gradle 失败" ——
这是必要的反面示例（anti-pattern），改成 ASCII 会削弱说服力。
所以审计脚本用 `KNOWN_EXAMPLE_DIRS` **显式放行**它，而不是放宽判据。

> **原则**：需要例外时，收紧判据 + 显式白名单，**不要为了少数情况放宽整体判据** ——
> 后者会让真正该抓的漏掉。

### ★ 三次迭代才把误报降到 0

| 轮次 | 命中 | 性质 | 修法 |
|---|---|---|---|
| 1 | 5 处 | 全是规则文本与占位符示例 | 判据从"出现过关键词"收紧为"像真值" |
| 2 | 4 处 | 全是 `E:\AndroidDev` 与必要的反面示例 | 判据改为"中文须在路径段内" + 显式白名单 |
| 3 | **0 处** | — | 判据与实现都固化成脚本 |

第 2 轮的根因值得记：正则 `[A-Z]:\+[^\\s]*[中文]` 中 `[^\\s]*` 能吞掉反引号与逗号，
于是 `E:\AndroidDev`，它是本` 也被当成"中文路径"。

> **可复用的教训**：写检查规则时，先想"它在**正确的**代码库上跑会报什么"。
> 一条在干净库上就有假阳性的规则，上线后会被当成噪音忽略掉。

### 判定规则

| 类别 | 保留？ | 理由 |
|---|---|---|
| 工具链路径（`<DevRoot>`，默认 `E:\AndroidDev`） | ✅ 保留 | 它是本 skill 的**默认值**，是文档的一部分 |
| 本机文件系统工具路径（`C:\gradle-home` 之类） | ✅ 可保留 | 与方法相关，且不指向某个人 |
| **项目相关路径**（含项目名/中文目录名） | ❌ 泛化 | 会暴露源项目身份 |
| 应用包名 / 应用名 / 业务领域 | ❌ 泛化 | 同上 |
| 真实上游端点、密钥 | ❌ 必须清 | 敏感 |
| 特定自然语言（除"中文"作为 CJK 的泛指） | ❌ 泛化 | 会指向源项目领域 |

### 泛化写法参考

| 原样 | 泛化后 |
|---|---|
| 含中文的项目真实路径 | `D:\工作\我的项目\app`（示例路径，纯示意） |
| 项目专用的 ASCII 短别名 | `E:\proj` |
| "A 语言 → B 语言翻译"（真实语言对） | "源语言 → 目标语言" |
| 真实的包名 / 应用名 | `<包名>` / `<应用名>` |

> ⚠️ **写这张表时本身也要用占位符**。
> 实测踩到过：把"真实别名 → 泛化写法"原样列在这里，
> 结果是**审计规则自己把要防的信息写进了仓库** ——
> 由审计脚本扫出来才发现（规则有效，但示例写错了）。
> 教"怎么泛化"不需要给出真实值，给形状就够了。

> **为什么值得单列一节**：这类信息在开发期读起来毫无问题（"就是我的项目啊"），
> 只有站在发布视角才觉得刺眼。所以要在**固定的动作里**检查它，
> 而不是指望写的时候一直绷着这根弦。

## 关于本机绝对路径

示例命令里出现 `E:\AndroidDev` 这类路径是可以的（它是本 skill 的默认值），
但**不要在文档正文里依赖某台机器的特定布局**。凡是可配置的地方，
说明清楚它是参数（如 `env.ps1 -DevRoot`）。
