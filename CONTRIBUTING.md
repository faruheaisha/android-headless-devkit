# 贡献指南

感谢有兴趣改进本 skill。本 skill 是一份**可执行的流程规范**，不是知识库，
因此对"事实准确性"和"可验证性"的要求高于一般文档。

## 两条硬性要求

### 1. 一切数值必须实测，不接受估算

本仓库里出现的每个数字（体积、耗时、内存、版本号、报错原文）都应当能追溯到
一次真实执行。写"大约 10 分钟"之前，先跑一遍；写"包体约 70MB"之前，先量一次。

**如果确实没有实测条件**，明确标注为"未验证"，不要让它看起来像实测值。

### 2. 脚本改动必须实跑

改 `scripts/` 或 `self_test.py` 后，至少执行：

```powershell
python .\self_test.py          # apk_report.py 的 23 项断言应全通过
python .\validate_skill.py     # SKILL.md 的 18 项结构断言应全通过
```

`validate_skill.py` 会检查 frontmatter 完整性与**引用无断链** ——
改过 `SKILL.md` 的目录树或文件名后尤其要跑它（重命名文件很容易漏改引用）。

涉及设备流程的改动（`verify_loop.ps1`），需要真跑一次装机验收：
先启动无头模拟器，再执行 `verify_loop.ps1`，确认截图非空、崩溃检查与
进程存活结论正确。

> 这不是形式要求。本仓库的 `verify_loop.ps1` 首版有两处真 bug
> （函数定义顺序、adb 输出未校验导致假阳性），**都是实跑才暴露的** ——
> 静态阅读代码不会发现。

## 目录约定

| 位置 | 放什么 | 不放什么 |
|---|---|---|
| `SKILL.md` | 入口：工作流、能力边界、硬性经验、待查清单 | 长篇方法论细节、可执行代码 |
| `references/` | 按阶段拆分的方法论全文 | 可执行代码 |
| `scripts/` | 可独立运行的脚本，不依赖私有数据或网络 | 业务逻辑、密钥 |
| `self_test.py` | 对 `scripts/` 的自动断言 | 需要真机才能跑的用例 |
| `evals/` | 触发与行为断言 | — |

## 写作风格

- **先给判据，再给做法**。"看到 X 就说明 Y"比"你可以尝试 Z"更有用。
- **症状 → 根因 → 解法**三段式组织坑点，便于检索。
- **保留报错原文**。原文比转述更有检索价值（别人会直接搜报错）。
- **写清"为什么这样做"**。尤其对于偏离直觉的做法
  （如"浅色底上的玻璃质感主要靠阴影而非透明度"）。
- 中文为主，技术名词保留英文原文。

## 新增坑点

坑点请加进 `references/06-pitfall-index.md` 对应分组，格式：

```
| 编号 | 症状（含报错原文关键词） | 根因 | 解法 |
```

若该坑点较深（需要展开代码或表格），在阶段文档里写详版，
速查表里放一行并链接过去。

## 提交前自检

- [ ] `python self_test.py` 全通过（23 项）
- [ ] `python validate_skill.py` 全通过（18 项，含引用无断链）
- [ ] 新增/修改的数值都是**实测**的，未实测的已标注
- [ ] 涉及设备流程的改动已**真跑过一次**装机验收
- [ ] 没有引入 APK、密钥、本机绝对路径等不应进仓库的内容
- [ ] 若改了 `SKILL.md` 的 `description` → 已过一遍 `evals/evals.json`
      （description 触发语境不全会导致 undertrigger，边界不写清会 scope creep）
- [ ] 变更已记入 `CHANGELOG.md`

## 关于本机绝对路径

示例命令里出现 `E:\AndroidDev` 这类路径是可以的（它是本 skill 的默认值），
但**不要在文档正文里依赖某台机器的特定布局**。凡是可配置的地方，
说明清楚它是参数（如 `env.ps1 -DevRoot`）。
